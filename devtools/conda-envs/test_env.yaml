name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - sphinx
  - sphinx_rtd_theme
  - flake8


    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
  #  - codecov
  - atomium

